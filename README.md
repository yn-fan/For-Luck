# 佛了 · For Luck

一个移动优先的原创网页仪式玩具：跟随节拍敲木鱼、积攒功德，或向无边海中放生鱼儿。

## 功能

- 木鱼与放生双模式
- 手动操作与自动操作
- `PERFECT / GOOD / COOL` 节拍判定与连击
- 连续 3 次 `PERFECT` 触发 10 秒特殊时刻
- 大号 `MISS` 反馈后延迟 500ms 揭签
- 随机六档吉凶签、中英双语关键词与“乱讲实验室”品牌贴纸
- Canvas 2D 动画、Web Audio 合成音效与响应式布局
- 功德、放生和声音设置保存在当前设备
- 键盘操作、减少动态和高对比模式支持

## 本地运行

项目不需要构建或安装依赖。在项目目录启动任意静态文件服务器：

```bash
python3 -m http.server 4173
```

然后访问 <http://localhost:4173>。

## 操作

- 点击或轻触画面：敲木鱼 / 放生
- `Space`：执行当前模式的操作
- `←` / `→`：切换木鱼与放生
- 自动敲 / 自动放生：开启当前模式的自动操作
- 声音：开启或关闭合成音效

## 技术

- Vanilla HTML、CSS、JavaScript
- Canvas 2D
- Web Audio API
- Local Storage
- 无第三方运行时依赖

## 数据说明

- 个人功德与放生记录仅保存在当前浏览器。
- 全球功德与全球放生数字为交互演示数据。
- 静态预览不会请求第三方定位服务；生产环境可通过同源接口提供所在地，失败时使用设备时区回退。

## 项目结构

```text
.
├── index.html
└── README.md
```
